function [outmat] = BigDcPProd_r(a,b)
% gives bigger jacobian of a*a*h wrt h

    N = length(a);

    a = [a;zeros(2*N,1)];
    b = [b;zeros(2*N,)]

    outmat = DcPProd(a,a);

    outmat(:,1:N) = zeros(3*N,N);
    outmat(N+1:end,N+1:end) = zeros(2*N,2*N);    

end

