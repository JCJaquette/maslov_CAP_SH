%% for verified pulse 1

%y0 = computeY0(A,a_bar,phi_cheb,params,ord,ICvec,del);
%z0 = computeZ0(A,Ad,ord,del);
%z1 = computeZ1(A_N,ord,phi_cheb,del,params); %approx .26 with ord=800, del= 1.005

%% for verified pulse 2

%y0 = computeY0(A,a_bar,phi_cheb,params,ord,ICvec,del);
%z0 = computeZ0(A,Ad,ord,del);
%z1 = computeZ1(A_N,ord,phi_cheb,del,params); %approx .18 with ord=1300,del=1.0033
                                                 

%% for verified pulse 3

%y0 = computeY0(A,a_bar,phi_cheb,params,ord,ICvec,del);
%z0 = computeZ0(A,Ad,ord,del);
%z1 = computeZ1(A_N,ord,phi_cheb,1.004,params); %approx .68 with ord = 1700, del = 1.004